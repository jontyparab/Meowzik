export default {
    currentSong(state) { return state.currentSong },
    songList(state) { return state.songList },
    background(state) { return state.background },
    backgroundName(state) { return state.backgroundName }
}